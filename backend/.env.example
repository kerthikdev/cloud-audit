# ── Environment Configuration ──────────────────────────────────────────────────
# Copy this to .env and fill in values. Never commit .env to git.

# Application
APP_ENV=development
APP_VERSION=1.0.0
LOG_LEVEL=INFO

# Database
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/cloudaudit

# AWS (leave empty to use mock mode)
MOCK_AWS=true
AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_PROFILE=
SCAN_REGIONS=us-east-1,us-west-2

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
